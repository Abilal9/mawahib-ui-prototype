# Development defaults (committed).
# Do NOT set EXPO_PUBLIC_API_URL here — Expo client dotenv would override
# `npm run start:railway`. API URL is chosen by npm scripts via app.config.js.
# Put Supabase secrets in gitignored `.env` only.
