# Production / release defaults (committed). Public API URL only — not a secret.
# Used when EXPO_PUBLIC_API_URL is not already set in the environment (EAS/CI).
# Dev switching still uses npm scripts + app.config.js — not this file during
# `expo start` (development mode).
EXPO_PUBLIC_API_URL=https://mawahib-backend-production.up.railway.app/api/v1
